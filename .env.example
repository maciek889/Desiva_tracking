# Database - PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/desiva?schema=public"

# JWT secret - generate with: openssl rand -base64 48
JWT_SECRET=""

# Upload directory (absolute path on server, or relative for dev)
UPLOAD_DIR="./public/uploads"

# Node environment
NODE_ENV="production"
